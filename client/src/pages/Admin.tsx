import { useAuth } from "@/_core/hooks/useAuth";
import { Button } from "@/components/ui/button";
import { PAYMENT_LABELS, formatDate, toDateInput } from "@/lib/invoice";
import { trpc } from "@/lib/trpc";
import { CalendarDays, ShieldCheck, UsersRound, X } from "lucide-react";
import { FormEvent, useEffect, useState } from "react";
import { toast } from "sonner";

type Method = "bkash" | "nagad" | "rocket" | "bank_transfer";
export default function Admin() {
  const { user } = useAuth(); const isAdmin = user?.role === "admin"; const utils = trpc.useUtils(); const usersQuery = trpc.admin.users.useQuery(undefined, { enabled: isAdmin }); const update = trpc.admin.updateSubscription.useMutation({ onSuccess: () => utils.admin.users.invalidate() }); const [selected, setSelected] = useState<any>(null); const [status, setStatus] = useState<"inactive" | "active" | "expired">("active"); const [activeUntil, setActiveUntil] = useState(""); const [method, setMethod] = useState<Method>("bkash"); const [note, setNote] = useState("");
  useEffect(() => { if (!selected) return; setStatus(selected.subscription?.status || "active"); setActiveUntil(toDateInput(selected.subscription?.activeUntil) || new Date(Date.now() + 30 * 86_400_000).toISOString().slice(0, 10)); setMethod(selected.subscription?.lastPaymentMethod || "bkash"); setNote(selected.subscription?.ownerNote || ""); }, [selected]);
  const save = async (event: FormEvent) => { event.preventDefault(); if (!selected) return; try { await update.mutateAsync({ userId: selected.user.id, status, activeUntil: activeUntil ? new Date(`${activeUntil}T12:00:00`).getTime() : null, lastPaymentMethod: method, ownerNote: note || null }); toast.success("Subscription updated"); setSelected(null); } catch (error) { toast.error(error instanceof Error ? error.message : "Could not update subscription"); } };
  if (!isAdmin) return <div className="empty-state"><ShieldCheck size={22} />This page is available only to the InvoicePro owner.</div>;
  const members = usersQuery.data ?? [];
  return <><section className="page-head"><div><p className="eyebrow">OWNER-ONLY CONTROLS</p><h1>Subscription admin</h1><p>Confirm local payment, then activate or extend access manually.</p></div></section><article className="surface-card"><div className="card-head"><div><p className="eyebrow">REGISTERED USERS</p><h2 className="mt-1">{members.length} account{members.length === 1 ? "" : "s"}</h2></div></div>{members.length ? <div className="invoice-table-wrap"><table className="invoice-table"><thead><tr><th>User</th><th>Business</th><th>Subscription</th><th>Active until</th><th /></tr></thead><tbody>{members.map(({ user: member, subscription, profile }: any) => <tr key={member.id}><td className="invoice-title-cell"><strong>{member.name || "Unnamed user"}</strong><span>{member.email || member.openId}</span></td><td>{profile?.businessName || "—"}</td><td><span className={`status-badge ${subscription?.status === "active" ? "status-paid" : subscription?.status === "expired" ? "status-overdue" : "status-draft"}`}>{subscription?.status || "inactive"}</span></td><td>{formatDate(subscription?.activeUntil)}</td><td className="text-right"><Button className="secondary-button h-9" onClick={() => setSelected({ user: member, subscription, profile })}>Manage</Button></td></tr>)}</tbody></table></div> : <div className="empty-state"><UsersRound size={22} />Registered accounts will appear here after they sign in.</div>}</article>{selected && <div className="fixed inset-0 z-50 grid place-items-end bg-[#102a43]/35 p-3 sm:place-items-center" onClick={() => setSelected(null)}><form className="w-full max-w-md rounded-[20px] bg-white p-5 shadow-2xl" onSubmit={save} onClick={event => event.stopPropagation()}><div className="card-head"><div><p className="eyebrow">MANUAL ACTIVATION</p><h2 className="mt-1">{selected.user.name || selected.user.email}</h2><p className="mt-1 text-xs text-slate-500">Confirm payment before granting access.</p></div><Button type="button" className="secondary-button icon-button" onClick={() => setSelected(null)}><X size={17} /></Button></div><div className="grid gap-3"><div className="field"><label>Subscription status</label><select value={status} onChange={e => setStatus(e.target.value as typeof status)}><option value="active">Active</option><option value="inactive">Inactive</option><option value="expired">Expired</option></select></div><div className="field"><label className="flex items-center gap-1"><CalendarDays size={13} />Active until</label><input type="date" value={activeUntil} onChange={e => setActiveUntil(e.target.value)} /></div><div className="field"><label>Payment confirmed through</label><select value={method} onChange={e => setMethod(e.target.value as Method)}>{Object.entries(PAYMENT_LABELS).map(([key, value]) => <option key={key} value={key}>{value}</option>)}</select></div><div className="field"><label>Owner note <span className="font-normal text-slate-400">(optional)</span></label><textarea value={note} onChange={e => setNote(e.target.value)} placeholder="Payment date, sender number, or internal note" /></div></div><div className="sticky-submit"><Button type="button" className="secondary-button" onClick={() => setSelected(null)}>Cancel</Button><Button type="submit" className="primary-button" disabled={update.isPending}>Save access</Button></div></form></div>}</>;
}
